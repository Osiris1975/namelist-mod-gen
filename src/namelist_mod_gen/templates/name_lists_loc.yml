l_english:
 {% for item in dict_item %}{% for id, data in item.items() %}name_list_{{ data.author }}_{{data.id}}:0 "{{ data.title }}"
 name_list_{{ data.author }}_{{data.id}}_desc:0 " "  
   {% endfor %}
 {% endfor %}
